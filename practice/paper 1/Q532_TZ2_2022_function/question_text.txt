The image contains the following text and mathematical content:

[Maximum mark: 8]

A function f is defined by f(x) = x√1-x² where -1 ≤ x ≤ 1.

The graph of y = f(x) is shown below.

(a) Show that f is an odd function.

The range of f is a ≤ y ≤ b, where a, b ∈ ℝ.

(b) Find the value of a and the value of b.