The image contains the following text:

[Maximum mark: 7]

The function f is defined for all x ∈ ℝ. The line with equation y = 6x - 1 is the tangent to the graph of f at x = 4.

(a) Write down the value of f'(4).

(b) Find f(4).

The function g is defined for all x ∈ ℝ where g(x) = x^2 - 3x and h(x) = f(g(x)).

(c) Find h(4).

(d) Hence find the equation of the tangent to the graph of h at x = 4.