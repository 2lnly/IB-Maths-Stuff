The text from the image is as follows:

(Question 1 continued)

(f) Hence, or otherwise, show that f_n'(a/4) > 0, for n ∈ ℤ⁺.

(g) By using the result from part (f) and considering the sign of f_n'(-1), show that the point (0, 0) on the graph of y = f_n(x) is:
(i) a local minimum point for even values of n, where n > 1 and a ∈ ℝ⁺;
(ii) a point of inflexion with zero gradient for odd values of n, where n > 1 and a ∈ ℝ⁺.

Consider the graph of y = x^(a - x)^k, where n ∈ ℤ⁺, a ∈ ℝ⁺ and k ∈ ℝ.

(h) State the conditions on n and k such that the equation x^(a - x)^k = k has four solutions for x.