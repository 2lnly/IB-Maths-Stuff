Here is the text extracted from the image, including all mathematical notation, equations, and symbols, preserving the structure and formatting:

4. [Maximum mark: 6]

The functions f and g are defined for x ∈ R by f(x) = 6x^2 - 12x + 1 and g(x) = -x + c, 
where c ∈ R.

(a) Find the range of f.

(b) Given that (g ∘ f)(x) ≤ 0 for all x ∈ R, determine the set of possible values for c.